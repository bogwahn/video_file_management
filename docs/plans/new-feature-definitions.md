one nice feature would be for remux to create a workfile of those files that connect be remux in their current state and will require a reencoding of its contents before any remux can take plcae. This worklfile will contain a list of the files that can't be remuxed along with the CLI command line that when executed succesfully will fully prepare the file to be remuxed along with a callack to remux that accomplishes the original remux goal.
